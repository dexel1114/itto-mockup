# AGENTS.md

このリポジトリで作業するAIエージェントへの指示です。タスクの内容にかかわらず、常にこの内容に従ってください。

---

## 最初にやること

**タスク開始時に、必ず `PROGRESS.md` を読んでください。** そこに5段階の進め方と「現在の段階」が書いてあります。今の段階の作業だけを行い、終わったら同じPRの中で `PROGRESS.md` を更新してください。

利用者から「続きを進めて」とだけ送られてきたら、それは「PROGRESS.mdの現在の段階を進めて」という意味です。

---

## 一緒に作業する人について

このリポジトリの利用者は**プログラミング未経験の事業責任者**です。不動産業界の実務には非常に詳しく、賃貸管理・売買仲介・リノベーション再販の経験があります。一方で、以下については知識がない前提で対応してください。

- Git、ブランチ、コミット、プルリクエストの仕組み
- コマンドライン、ターミナル
- プログラミング言語、フレームワーク、ライブラリ
- サーバー、データベース、デプロイ

**この人が判断できるのは「事業として正しいか」「不動産の実務として自然か」であり、「技術的にどう作るか」ではありません。** 判断を仰ぐときは、必ず事業側の言葉に翻訳してください。

---

## 応答のしかた

### 言語

すべて日本語で応答してください。コード内のコメントも日本語で書いてください。

### 専門用語

技術用語を使うときは、必ず一言で説明を添えてください。

- 悪い例：「リポジトリのルートに配置しました」
- 良い例：「一番上の階層（フォルダを開いてすぐ見える場所）に置きました」

### 作業が終わったら

毎回、応答の最後に以下の3つを書いてください。

1. **何をしたか**（技術的な詳細ではなく、結果として何がどう変わったか）
2. **次にこの人が押すボタン**（画面のどこにある何を押せばいいか、具体的に）
3. **確認してほしいこと**（あれば）

例：

```
【やったこと】
物件一覧の画面と、オファー入力フォームの画面を作りました。
ダミーの物件データを5件入れてあります。

【次にすること】
1. この下にある「Create PR」を押してください
2. GitHubの画面が開くので、緑色の「Merge pull request」を押してください
3. さらに「Confirm merge」を押すと保存されます

【確認してほしいこと】
オファーフォームの項目の並び順は、実務的に自然でしょうか。
価格を最初に聞くか、資金方法を先に聞くかで迷いました。
```

### 質問のしかた

- 一度に質問するのは**最大3つまで**。それ以上ある場合は重要な順に3つだけ聞き、残りは次回に回してください
- 技術的な選択肢ではなく、**事業上の選択肢**として提示してください
  - 悪い例：「状態管理をどう実装しますか」
  - 良い例：「入札の履歴を、買主にも見せますか。売主だけに見せますか」
- 選択肢には、それぞれ「こうすると何が起きるか」を1行添えてください
- 自分の推奨があれば、理由とともに書いてください。ただし推奨を押し付けず、決めるのは相手です

### やってはいけないこと

- 相手が理解できない前提で話を進めること
- 「〜はご存知だと思いますが」と書くこと
- エラーメッセージをそのまま貼って終わること（何が起きたかを日本語で説明する）
- 相手に手動でファイルを作らせたり、コマンドを打たせたりすること（すべてこちらで行う）

---

## 仕様の扱いかた

### 4つの区分を混同しない

`spec/02-kettei-jikou.md` は以下の4区分に分かれています。この区分は絶対に守ってください。

| 区分 | 扱い |
|---|---|
| 【確定】 | 実装してよい |
| 【推奨案・回答待ち】 | **決定ではない。** 実装前に確認する |
| 【未確定】 | **誰も決めていない。** 推測で埋めない |
| 【今回作らない】 | 実装しない |

`spec/02-kettei-jikou.md` の末尾には「検討過程で却下された案」の章があります。**ここに書かれた案を要件として拾わないでください。**

### 未確定のまま進める必要があるとき

どうしても仮置きが必要な場合は、

1. 推奨案があればそれを採用する
2. 画面上に「※仮：この仕様は未確定です」と表示する
3. `spec/20-mockup-guide.md` の「仮置きした箇所」に記録する
4. 応答の中で、何を仮置きしたか明示する

黙って決めないでください。

### 決定が変わったとき

利用者から「この項目はこう決まりました」と伝えられた場合、

1. `spec/02-kettei-jikou.md` の該当箇所を【推奨案・回答待ち】から【確定】に移す
2. 変更前の内容も「〜だったが〜に決定」の形で残す（履歴を消さない）
3. 関連する画面とドキュメントを更新する

---

## 作るものの前提

### モックアップの位置づけ

このリポジトリで作るのは、**見た目だけのモックアップ**です。掲載会社への営業や投資家への説明に使います。

- ログイン認証、決済、実際の入札処理は作らない
- データはすべてJavaScript内の固定値（ダミーデータ）
- データベース、サーバーサイドの処理は使わない

### 技術的な制約

- HTML / CSS / JavaScript のみ
- React、Vue、Next.js などのフレームワークは使わない
- ビルド作業が不要な構成にする（`index.html` をダブルクリックすれば動く状態を維持する）
- リポジトリのルートに `index.html` を置く
- 日本語UI。スマートフォンの画面幅でも崩れないこと
- 高額不動産を扱うサービスなので、落ち着いた配色にする

**この制約は必ず守ってください。** 利用者はビルド作業ができないため、フレームワークを導入すると画面が見られなくなります。

---

## ドキュメント

コードを変更したら、**必ず**関連するドキュメントも同じPRで更新してください。指示がなくても更新してください。

| ファイル | 内容 |
|---|---|
| `README.md` | このリポジトリの概要、モックアップの見方、画面一覧 |
| `spec/20-mockup-guide.md` | 各画面の役割、ファイルと画面の対応表、仮置きした箇所の一覧 |
| `spec/21-dummy-data.md` | ダミーデータの構造、本番で必要になるデータの一覧 |

このリポジトリの成果物は、将来的に開発会社への発注資料になります。**「なぜそう作ったか」が残っていないと価値が半減します。**

---

## 用語の統一

`spec/13-yougo.md` の定義に従ってください。特に以下は混同しないこと。

- **落札**：オークションで最高額の入札者が決まること。取引の成立ではない
- **成約**：契約・決済が完了し、取引が実際に成立すること

この2つは別のデータとして記録します。
